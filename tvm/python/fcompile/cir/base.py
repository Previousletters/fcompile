class Placeholder:

    class F:

        @staticmethod
        def ones(h, w, c):
            pass

        @staticmethod
        def zeros(h, w, c):
            pass

        @staticmethod
        def pad(h, w, c):
            pass

    ones_zeros = "ones_zeros"
